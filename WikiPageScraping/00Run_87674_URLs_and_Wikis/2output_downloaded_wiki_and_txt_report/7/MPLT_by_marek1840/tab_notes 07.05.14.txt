[<div class="wiki-body gollum-markdown-content instapaper_body" id="wiki-body">
        <div class="markdown-body">
          <p>To do:</p>
<ul>
<li>Use brackets in IF statements if needed</li>
<li>Change 'var' to something else (probably just delete and group)</li>
<li>Fix bug with missed default case</li>
<li>Add newlines</li>
<li>Fix bug with translator translating '_' in variables' names</li>
<li>Add translation tests</li>
</ul>
<p>Ideas:</p>
<ul>
<li>Appending AMPL code (as comment) to LaTeX file (execution option)</li>
<li>Id prefix in macros (execution option)</li>
<li>Add indexing for FORALL marker (\mathop\forall)</li>
</ul>

        </div>

    </div>]