[<div class="wiki-body gollum-markdown-content instapaper_body" id="wiki-body">
        <div class="markdown-body">
          <p>##This is a file about git config
####* Usage
In home directory, new file .gitconfig, then copy the following content to specified file(.gitconfig).</p>
<pre><code>[user]
    name = zuopengl
    email = 471347111@qq.com
[alias]
    co = checkout
    br = branch
    mg = merge
    ci = commit
    st = status
    line = log --oneline
    diff = diff
    diffc = diff --cached
    logp = log --pretty=raw
[http]
    sslVerify = false
</code></pre>

        </div>

    </div>]