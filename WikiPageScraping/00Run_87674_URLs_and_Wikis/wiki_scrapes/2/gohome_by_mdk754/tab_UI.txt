[<div class="wiki-body gollum-markdown-content instapaper_body" id="wiki-body">
        <div class="markdown-body">
          <p>The UI will exist as a separate process which talks to the core over the api, and exposes it's own web server to hand out pages. The UI will be entirely web browser based.</p>
<p>A UI does not currently exist for GoHome, but the intent will be to keep it in the same repository as the core, using Go templates for UI customization.</p>
<p><em>This page is a work in progress and needs attention.</em></p>

        </div>

    </div>]