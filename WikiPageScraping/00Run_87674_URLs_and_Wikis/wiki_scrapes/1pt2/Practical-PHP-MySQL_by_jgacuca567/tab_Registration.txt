[<div class="wiki-body gollum-markdown-content instapaper_body" id="wiki-body">
        <div class="markdown-body">
          <p>register-page.php - This page is used to register a user, the information being stored in the database is the user's first name, last name, email, password.</p>
<p>register-view-users.php - This page shows the users that have been registered. Using the SQL Statements the first name and last name is Concatenated and becomes Both Names displayed together as well as the date the user was registered.</p>
<p>register-password.php - This page can be used to change the password of a user, although it is not quite secure, the user can change the password using the old password, for it to work the user must know the old password then change it to new password.</p>

        </div>

    </div>]